# === Narzędzia deweloperskie / środowisko (nie wersjonowane) ===
.claude/
.mcp.json
.devcontainer/
.vscode/

# === Docker i budowanie obrazu ===
Dockerfile
build.sh
build.number
build.number.lock
*.dockerignore
Directory.Build.props

# === Logi ===
logs/
*.log

# === Build artifacts .NET ===
obj/
bin/

# === Stare repozytoria podprojektów (zachowane jako backup) ===
**/.git.bak/

# === Per-projektowe .gitignore (nie potrzebne w monorepo) ===
src/Saga.Model/.gitignore
src/Saga.Cli/.gitignore
src/Saga.Core/.gitignore
WordParserApi/.gitignore

# === Pozostałe ===
*.bat
push-all.sh
kopie*/
810*/
*Example*.cs
docs.*.md

# === Tymczasowe pliki Word ===
~$*

# === Repozytorium dokumentów roboczych ===
DocRepo/

# === Lokalne lustro zewnętrznej schemy (tylko do odczytu) + narzędzie i metadane ===
schema-pg/
schema/.upstream.txt
tools/sync-schema-pg.sh

# === Lokalne artefakty testowe (nie wersjonowane) ===
tests/Saga.Core.Tests/Artifacts/*.docx
tests/Saga.Core.Tests/Artifacts/*.xml

# === Projekty zawieszone ===
WordParserApi/

# === Artefakty ===
Artifacts/
docs/internal/

# Raporty narzędzia CorpusValidator (generowane do bieżącego katalogu)
corpus-report.*
